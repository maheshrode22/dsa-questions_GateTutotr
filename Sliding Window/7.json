{
  "problems": [
    {
      "id": null,
      "title": "Longest Substring with At Most K Distinct Characters",
      "description": "Given a string s and an integer K, find the length of the longest substring that contains at most K distinct characters.\n\nInput Format:\nFirst line: string s (no spaces).\nSecond line: integer K.\n\nOutput Format:\nPrint a single integer: the length of the longest valid substring.",
      "examples": "Sample Input\naraaci\n2\n\nSample Output\n4\n\nExplanation:\nThe longest substring with at most 2 distinct characters is 'araa' with length 4.",
      "constraints": "1 <= |s| <= 100000\n1 <= K <= 26",
      "hints": 2,
      "timeLimit": 1,
      "memoryLimit": 256,
      "subdomainId": 2033,
      "difficulty": 2,
      "testCases": [
        {
          "input": "araaci\n2",
          "expectedOutput": "4"
        },
        {
          "input": "araaci\n1",
          "expectedOutput": "2"
        },
        {
          "input": "cbbebi\n3",
          "expectedOutput": "5"
        },
        {
          "input": "aaaa\n2",
          "expectedOutput": "4"
        },
        {
          "input": "abc\n1",
          "expectedOutput": "1"
        }
      ],
      "starterCodes": [
        {
          "language": 1,
          "code": "class Solution:\n    def longestSubstringAtMostKDistinct(self, s, K):\n        # TODO: Return length of the longest substring with at most K distinct characters\n        pass\n\nif __name__ == '__main__':\n    s = input().strip()\n    K = int(input().strip())\n    sol = Solution()\n    print(sol.longestSubstringAtMostKDistinct(s, K))"
        },
        {
          "language": 2,
          "code": "import java.io.*;\nimport java.util.*;\n\nclass Solution {\n    public int longestSubstringAtMostKDistinct(String s, int K) {\n        // TODO: Return length of the longest substring with at most K distinct characters\n        return 0;\n    }\n\n    public static void main(String[] args) throws Exception {\n        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));\n        String s = br.readLine().trim();\n        int K = Integer.parseInt(br.readLine().trim());\n        Solution sol = new Solution();\n        System.out.println(sol.longestSubstringAtMostKDistinct(s, K));\n    }\n}"
        },
        {
          "language": 3,
          "code": "class Solution {\n    longestSubstringAtMostKDistinct(s, K) {\n        // TODO: Return length of the longest substring with at most K distinct characters\n        return 0;\n    }\n}\n\nfunction solve(input) {\n    const lines = input.trim().split(/\\n/);\n    const s = lines[0].trim();\n    const K = parseInt(lines[1].trim());\n    const sol = new Solution();\n    console.log(sol.longestSubstringAtMostKDistinct(s, K));\n}\n\nconst fs = require('fs');\nsolve(fs.readFileSync(0, 'utf-8'));"
        },
        {
          "language": 4,
          "code": "#include <bits/stdc++.h>\nusing namespace std;\n\nclass Solution {\npublic:\n    int longestSubstringAtMostKDistinct(const string& s, int K) {\n        // TODO: Return length of the longest substring with at most K distinct characters\n        return 0;\n    }\n};\n\nint main() {\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    string s;\n    if (!(cin >> s)) return 0;\n    int K;\n    if (!(cin >> K)) return 0;\n    Solution sol;\n    cout << sol.longestSubstringAtMostKDistinct(s, K);\n    return 0;\n}"
        },
        {
          "language": 5,
          "code": "#include <stdio.h>\n#include <string.h>\n\nint longestSubstringAtMostKDistinct(const char* s, int K) {\n    // TODO: Return length of the longest substring with at most K distinct characters\n    return 0;\n}\n\nint main() {\n    char s[100005];\n    if (scanf(\"%s\", s) != 1) return 0;\n    int K;\n    if (scanf(\"%d\", &K) != 1) return 0;\n    printf(\"%d\", longestSubstringAtMostKDistinct(s, K));\n    return 0;\n}"
        }
      ],
      "hintsList": [
        "Use a sliding window and a frequency map to track the count of each character.",
        "If the number of distinct characters exceeds K, move the left pointer and decrease counts until it becomes at most K again."
      ]
    }
  ]
}
