{
  "problems": [
    {
      "id": null,
      "title": "Max Window Sum",
      "description": "You are given an array of integers and a window size k. Your task is to find the maximum sum of any contiguous subarray of size k. If k is greater than the array size, print 0.\n\nInput Format:\nFirst line: two integers n and k.\nSecond line: n space-separated integers.\n\nOutput Format:\nPrint a single integer: the maximum sum of any window of size k, or 0 if such window does not exist.",
      "examples": "Sample Input\n7 3\n2 5 -1 7 3 -1 2\n\nSample Output\n11\n\nExplanation:\nThe maximum sum window is [5, -1, 7] with sum 11.",
      "constraints": "1 <= n <= 100000\n1 <= k <= 100000\n-10^9 <= a[i] <= 10^9",
      "hints": 2,
      "timeLimit": 1,
      "memoryLimit": 256,
      "subdomainId": 2033,
      "difficulty": 1,
      "testCases": [
        {
          "input": "7 3\n2 5 -1 7 3 -1 2",
          "expectedOutput": "11"
        },
        {
          "input": "5 2\n1 2 3 4 5",
          "expectedOutput": "9"
        },
        {
          "input": "5 5\n-1 -2 -3 -4 -5",
          "expectedOutput": "-15"
        },
        {
          "input": "3 4\n1 2 3",
          "expectedOutput": "0"
        },
        {
          "input": "6 1\n-5 -1 0 2 10 -3",
          "expectedOutput": "10"
        }
      ],
      "starterCodes": [
        {
          "language": 1,
          "code": "class Solution:\n    def maxWindowSum(self, arr, n, k):\n        # TODO: Return maximum sum of any window of size k\n        pass\n\nif __name__ == '__main__':\n    n_k = input().split()\n    n = int(n_k[0])\n    k = int(n_k[1])\n    arr = list(map(int, input().split()))\n    sol = Solution()\n    print(sol.maxWindowSum(arr, n, k))"
        },
        {
          "language": 2,
          "code": "import java.io.*;\nimport java.util.*;\n\nclass Solution {\n    public long maxWindowSum(int[] arr, int n, int k) {\n        // TODO: Return maximum sum of any window of size k\n        return 0L;\n    }\n\n    public static void main(String[] args) throws Exception {\n        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));\n        String[] parts = br.readLine().trim().split(\" \");\n        int n = Integer.parseInt(parts[0]);\n        int k = Integer.parseInt(parts[1]);\n        String[] arrStr = br.readLine().trim().split(\" \");\n        int[] arr = new int[n];\n        for (int i = 0; i < n; i++) arr[i] = Integer.parseInt(arrStr[i]);\n        Solution sol = new Solution();\n        System.out.println(sol.maxWindowSum(arr, n, k));\n    }\n}"
        },
        {
          "language": 3,
          "code": "class Solution {\n    maxWindowSum(arr, n, k) {\n        // TODO: Return maximum sum of any window of size k\n        return 0;\n    }\n}\n\nfunction solve(input) {\n    const lines = input.trim().split(/\\n/);\n    const first = lines[0].trim().split(/\\s+/);\n    const n = parseInt(first[0]);\n    const k = parseInt(first[1]);\n    const arr = lines[1].trim().split(/\\s+/).map(Number);\n    const sol = new Solution();\n    console.log(sol.maxWindowSum(arr, n, k));\n}\n\nconst fs = require('fs');\nsolve(fs.readFileSync(0, 'utf-8'));"
        },
        {
          "language": 4,
          "code": "#include <bits/stdc++.h>\nusing namespace std;\n\nclass Solution {\npublic:\n    long long maxWindowSum(vector<long long>& arr, int n, int k) {\n        // TODO: Return maximum sum of any window of size k\n        return 0;\n    }\n};\n\nint main() {\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    int n, k;\n    if (!(cin >> n >> k)) return 0;\n    vector<long long> arr(n);\n    for (int i = 0; i < n; i++) cin >> arr[i];\n    Solution sol;\n    cout << sol.maxWindowSum(arr, n, k);\n    return 0;\n}"
        },
        {
          "language": 5,
          "code": "#include <stdio.h>\n\nlong long maxWindowSum(long long arr[], int n, int k) {\n    // TODO: Return maximum sum of any window of size k\n    return 0;\n}\n\nint main() {\n    int n, k;\n    if (scanf(\"%d %d\", &n, &k) != 2) return 0;\n    long long arr[100000];\n    for (int i = 0; i < n; i++) scanf(\"%lld\", &arr[i]);\n    printf(\"%lld\", maxWindowSum(arr, n, k));\n    return 0;\n}"
        }
      ],
      "hintsList": [
        "Compute the sum of the first window of size k directly.",
        "Slide the window by one element at a time: subtract the element leaving the window and add the new element entering it."
      ]
    }
  ]
}
