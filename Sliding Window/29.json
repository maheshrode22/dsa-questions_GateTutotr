{
  "problems": [
    {
      "id": null,
      "title": "Sliding Window Median (Sliding Window)",
      "description": "The median is the middle value in an ordered integer list. If the size of the list is even, there is no middle value. So the median is the mean of the two middle values.\nYou are given an integer array nums and an integer k. There is a sliding window of size k which is moving from the very left of the array to the very right. Return the median array for each window in the original array.\n\nInput Format:\nFirst line: two integers n and k.\nSecond line: n space-separated integers.\n\nOutput Format:\nPrint the medians on a single line, separated by spaces. Format each median to 5 decimal places.",
      "examples": "Sample Input\n8 3\n1 3 -1 -3 5 3 6 7\n\nSample Output\n1.00000 -1.00000 -1.00000 3.00000 5.00000 6.00000",
      "constraints": "1 <= k <= nums.length <= 10^5\n-2^31 <= nums[i] <= 2^31 - 1",
      "hints": 2,
      "timeLimit": 1,
      "memoryLimit": 256,
      "subdomainId": 2033,
      "difficulty": 3,
      "testCases": [
        {
          "input": "8 3\n1 3 -1 -3 5 3 6 7",
          "expectedOutput": "1.00000 -1.00000 -1.00000 3.00000 5.00000 6.00000"
        },
        {
          "input": "9 3\n1 2 3 4 2 3 1 4 2",
          "expectedOutput": "2.00000 3.00000 3.00000 3.00000 2.00000 3.00000 2.00000"
        },
        {
          "input": "4 2\n1 2 3 4",
          "expectedOutput": "1.50000 2.50000 3.50000"
        },
        {
          "input": "5 4\n1 2 3 4 5",
          "expectedOutput": "2.50000 3.50000"
        },
        {
          "input": "1 1\n10",
          "expectedOutput": "10.00000"
        }
      ],
      "starterCodes": [
        {
          "language": 1,
          "code": "class Solution:\n    def medianSlidingWindow(self, nums, k):\n        # TODO: Return the median array for each window\n        pass\n\nif __name__ == '__main__':\n    line1 = input().split()\n    n, k = int(line1[0]), int(line1[1])\n    nums = list(map(int, input().split()))\n    sol = Solution()\n    res = sol.medianSlidingWindow(nums, k)\n    print(' '.join(f'{x:.5f}' for x in res))"
        },
        {
          "language": 2,
          "code": "import java.util.*;\nimport java.io.*;\n\nclass Solution {\n    public double[] medianSlidingWindow(int[] nums, int k) {\n        // TODO: Return the median array for each window\n        return null;\n    }\n\n    public static void main(String[] args) throws Exception {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        int k = sc.nextInt();\n        int[] nums = new int[n];\n        for(int i=0; i<n; i++) nums[i] = sc.nextInt();\n        Solution sol = new Solution();\n        double[] res = sol.medianSlidingWindow(nums, k);\n        for (int i = 0; i < res.length; i++) System.out.printf(\"%.5f%s\", res[i], (i == res.length - 1 ? \"\" : \" \"));\n    }\n}"
        },
        {
          "language": 3,
          "code": "class Solution {\n    medianSlidingWindow(nums, k) {\n        // TODO: Return the median array for each window\n        return null;\n    }\n}\n\nfunction main() {\n    const fs = require('fs');\n    const lines = fs.readFileSync(0, 'utf-8').trim().split('\\n');\n    const [n, k] = lines[0].trim().split(/\\s+/).map(Number);\n    const nums = lines[1].trim().split(/\\s+/).map(Number);\n    const sol = new Solution();\n    const res = sol.medianSlidingWindow(nums, k);\n    console.log(res.map(x => x.toFixed(5)).join(' '));\n}\n\nmain();"
        },
        {
          "language": 4,
          "code": "#include <iostream>\n#include <vector>\n#include <string>\n#include <algorithm>\n#include <unordered_map>\n#include <map>\n#include <set>\n#include <queue>\n\nusing namespace std;\n\nclass Solution {\npublic:\n    vector<double> medianSlidingWindow(vector<int>& nums, int k) {\n        // TODO: Return the median array for each window\n        return {};\n    }\n};\n\nint main() {\n    ios::sync_with_stdio(false);\n    cin.tie(NULL);\n    int n, k;\n    cin >> n >> k;\n    vector<int> nums(n);\n    for(int i=0; i<n; i++) cin >> nums[i];\n    Solution sol;\n    vector<double> res = sol.medianSlidingWindow(nums, k);\n    for(int i=0; i<res.size(); ++i) printf(\"%.5f%s\", res[i], (i == res.size()-1 ? \"\" : \" \"));\n    return 0;\n}"
        },
        {
          "language": 5,
          "code": "#include <stdio.h>\n#include <stdlib.h>\n#include <string.h>\n\nint main() {\n    int n, k;\n    scanf(\"%d %d\", &n, &k);\n    int *nums = (int*)malloc(n * sizeof(int));\n    for(int i=0; i<n; i++) scanf(\"%d\", &nums[i]);\n    // TODO: Return the median array for each window\n    return 0;\n}"
        }
      ],
      "hintsList": [
        "Think about how to use a sliding window to optimize the solution.",
        "Consider using a frequency map or two pointers to track the window state."
      ]
    }
  ]
}