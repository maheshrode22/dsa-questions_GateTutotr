{
  "problems": [
    {
      "id": null,
      "title": "Count Even Numbers in Each Window",
      "description": "Given an array of integers and a window size k, print the number of even numbers in every contiguous subarray (window) of size k. If k is greater than n, print nothing.\n\nInput Format:\nFirst line: two integers n and k.\nSecond line: n space-separated integers.\n\nOutput Format:\nPrint the count of even numbers for each window of size k on a single line, separated by spaces.",
      "examples": "Sample Input\n5 3\n1 2 4 5 6\n\nSample Output\n2 1 2\n\nExplanation:\nWindows: [1,2,4] -> 2 evens, [2,4,5] -> 2 evens, [4,5,6] -> 2 evens. (Your implementation should match the definition of even numbers in the window.)",
      "constraints": "1 <= n <= 100000\n1 <= k <= 100000\n-10^9 <= a[i] <= 10^9",
      "hints": 2,
      "timeLimit": 1,
      "memoryLimit": 256,
      "subdomainId": 2033,
      "difficulty": 1,
      "testCases": [
        {
          "input": "5 3\n1 2 4 5 6",
          "expectedOutput": "2 2 2"
        },
        {
          "input": "4 2\n2 4 6 8",
          "expectedOutput": "2 2 2"
        },
        {
          "input": "4 3\n1 3 5 7",
          "expectedOutput": "0 0"
        },
        {
          "input": "3 4\n1 2 3",
          "expectedOutput": ""
        },
        {
          "input": "6 3\n0 -2 3 5 8 10",
          "expectedOutput": "2 1 1 2"
        }
      ],
      "starterCodes": [
        {
          "language": 1,
          "code": "class Solution:\n    def countEvenInWindows(self, arr, n, k):\n        # TODO: Return a list of counts of even numbers for each window of size k\n        pass\n\nif __name__ == '__main__':\n    n_k = input().split()\n    n = int(n_k[0])\n    k = int(n_k[1])\n    arr = list(map(int, input().split()))\n    sol = Solution()\n    res = sol.countEvenInWindows(arr, n, k)\n    print(' '.join(map(str, res))) if res else print('', end='')"
        },
        {
          "language": 2,
          "code": "import java.io.*;\nimport java.util.*;\n\nclass Solution {\n    public List<Integer> countEvenInWindows(int[] arr, int n, int k) {\n        // TODO: Return list of counts of even numbers for each window of size k\n        return new ArrayList<>();\n    }\n\n    public static void main(String[] args) throws Exception {\n        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));\n        String[] first = br.readLine().trim().split(\" \");\n        int n = Integer.parseInt(first[0]);\n        int k = Integer.parseInt(first[1]);\n        String[] parts = br.readLine().trim().split(\" \");\n        int[] arr = new int[n];\n        for (int i = 0; i < n; i++) arr[i] = Integer.parseInt(parts[i]);\n        Solution sol = new Solution();\n        List<Integer> res = sol.countEvenInWindows(arr, n, k);\n        for (int i = 0; i < res.size(); i++) {\n            if (i > 0) System.out.print(\" \");\n            System.out.print(res.get(i));\n        }\n    }\n}"
        },
        {
          "language": 3,
          "code": "class Solution {\n    countEvenInWindows(arr, n, k) {\n        // TODO: Return array of counts of even numbers for each window of size k\n        return [];\n    }\n}\n\nfunction solve(input) {\n    const lines = input.trim().split(/\\n/);\n    const first = lines[0].trim().split(/\\s+/);\n    const n = parseInt(first[0]);\n    const k = parseInt(first[1]);\n    const arr = lines[1].trim().split(/\\s+/).map(Number);\n    const sol = new Solution();\n    const res = sol.countEvenInWindows(arr, n, k);\n    if (res.length > 0) {\n        console.log(res.join(' '));\n    } else {\n        console.log('');\n    }\n}\n\nconst fs = require('fs');\nsolve(fs.readFileSync(0, 'utf-8'));"
        },
        {
          "language": 4,
          "code": "#include <bits/stdc++.h>\nusing namespace std;\n\nclass Solution {\npublic:\n    vector<int> countEvenInWindows(const vector<long long>& arr, int n, int k) {\n        // TODO: Return counts of even numbers for each window of size k\n        return {};\n    }\n};\n\nint main() {\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    int n, k;\n    if (!(cin >> n >> k)) return 0;\n    vector<long long> arr(n);\n    for (int i = 0; i < n; i++) cin >> arr[i];\n    Solution sol;\n    vector<int> res = sol.countEvenInWindows(arr, n, k);\n    for (size_t i = 0; i < res.size(); i++) {\n        if (i) cout << ' ';\n        cout << res[i];\n    }\n    return 0;\n}"
        },
        {
          "language": 5,
          "code": "#include <stdio.h>\n\nint main() {\n    int n, k;\n    if (scanf(\"%d %d\", &n, &k) != 2) return 0;\n    long long arr[100000];\n    for (int i = 0; i < n; i++) scanf(\"%lld\", &arr[i]);\n    // TODO: Compute counts of even numbers for each window of size k and print them space-separated.\n    return 0;\n}"
        }
      ],
      "hintsList": [
        "Maintain the count of even numbers in the current window instead of recounting each time.",
        "When you slide the window, update the count by checking the element leaving and the element entering the window."
      ]
    }
  ]
}