{
  "problems": [
    {
      "id": null,
      "title": "Longest Subarray with Sum At Most K",
      "description": "Given an array of non-negative integers and an integer K, find the length of the longest contiguous subarray whose sum is less than or equal to K.\n\nInput Format:\nFirst line: two integers n and K.\nSecond line: n space-separated non-negative integers.\n\nOutput Format:\nPrint a single integer: the maximum length of such subarray.",
      "examples": "Sample Input\n5 7\n2 1 5 1 3\n\nSample Output\n3\n\nExplanation:\nThe longest subarray with sum <= 7 is [1,5,1] with length 3.",
      "constraints": "1 <= n <= 100000\n0 <= a[i] <= 10^9\n0 <= K <= 10^14",
      "hints": 2,
      "timeLimit": 1,
      "memoryLimit": 256,
      "subdomainId": 2033,
      "difficulty": 2,
      "testCases": [
        {
          "input": "5 7\n2 1 5 1 3",
          "expectedOutput": "3"
        },
        {
          "input": "5 4\n2 2 2 2 2",
          "expectedOutput": "2"
        },
        {
          "input": "4 3\n4 1 1 1",
          "expectedOutput": "3"
        },
        {
          "input": "5 0\n0 0 0 0 0",
          "expectedOutput": "5"
        },
        {
          "input": "3 5\n6 7 8",
          "expectedOutput": "0"
        }
      ],
      "starterCodes": [
        {
          "language": 1,
          "code": "class Solution:\n    def longestSubarraySumAtMostK(self, arr, n, K):\n        # TODO: Return the length of the longest contiguous subarray whose sum is <= K\n        pass\n\nif __name__ == '__main__':\n    n, K = map(int, input().split())\n    arr = list(map(int, input().split()))\n    sol = Solution()\n    print(sol.longestSubarraySumAtMostK(arr, n, K))"
        },
        {
          "language": 2,
          "code": "import java.util.*;\nimport java.io.*;\n\nclass Solution {\n    public int longestSubarraySumAtMostK(int[] arr, int n, long K) {\n        // TODO: Return the length of the longest contiguous subarray whose sum is <= K\n        return 0;\n    }\n\n    public static void main(String[] args) throws Exception {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        long K = sc.nextLong();\n        int[] arr = new int[n];\n        for(int i=0; i<n; i++) arr[i] = sc.nextInt();\n        Solution sol = new Solution();\n        System.out.println(sol.longestSubarraySumAtMostK(arr, n, K));\n    }\n}"
        },
        {
          "language": 3,
          "code": "class Solution {\n    longestSubarraySumAtMostK(arr, n, K) {\n        // TODO: Return the length of the longest contiguous subarray whose sum is <= K\n        return null;\n    }\n}\n\nfunction main() {\n    const fs = require('fs');\n    const lines = fs.readFileSync(0, 'utf-8').trim().split('\\n');\n    const [n, K] = lines[0].trim().split(/\\s+/).map(Number);\n    const arr = lines[1].trim().split(/\\s+/).map(Number);\n    const sol = new Solution();\n    console.log(sol.longestSubarraySumAtMostK(arr, n, K));\n}\n\nmain();"
        },
        {
          "language": 4,
          "code": "#include <iostream>\n#include <vector>\n#include <string>\n#include <algorithm>\n#include <unordered_map>\n#include <map>\n#include <set>\n#include <queue>\n\nusing namespace std;\n\nclass Solution {\npublic:\n    int longestSubarraySumAtMostK(vector<int>& arr, int n, long long K) {\n        // TODO: Return the length of the longest contiguous subarray whose sum is <= K\n        return {};\n    }\n};\n\nint main() {\n    ios::sync_with_stdio(false);\n    cin.tie(NULL);\n    int n;\n    long long K;\n    cin >> n >> K;\n    vector<int> arr(n);\n    for(int i=0; i<n; i++) cin >> arr[i];\n    Solution sol;\n    cout << sol.longestSubarraySumAtMostK(arr, n, K) << endl;\n    return 0;\n}"
        },
        {
          "language": 5,
          "code": "#include <stdio.h>\n#include <stdlib.h>\n#include <string.h>\n\nint main() {\n    int n;\n    long long K;\n    scanf(\"%d %lld\", &n, &K);\n    int *arr = (int*)malloc(n * sizeof(int));\n    for(int i=0; i<n; i++) scanf(\"%d\", &arr[i]);\n    // TODO: Return the length of the longest contiguous subarray whose sum is <= K\n    return 0;\n}"
        }
      ],
      "hintsList": [
        "Use two pointers (sliding window) and maintain the current sum.",
        "While the sum is greater than K, move the left pointer forward and subtract elements from the sum, updating the maximum window length when the sum is <= K."
      ]
    }
  ]
}